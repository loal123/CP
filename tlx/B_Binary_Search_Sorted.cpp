#include <bits/stdc++.h>
using namespace std;
vector<int> v;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, q;
    cin >> n;
    while (n--)
    {
        int z;
        cin >> z;
        v.push_back(z);
    }
    sort(v.begin(), v.end());

    cin >> q;
    while (q--)
    {
        int m;
        cin >> m;
        
    }
}