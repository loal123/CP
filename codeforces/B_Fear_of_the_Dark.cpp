#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long double ld;
#define pii pair<int, int>
#define pll pair<ll, ll>
#define vi vector<int>
#define vl vector<ll>
#define fi first
#define se second
#define pb push_back
#define mp make_pair
const ll mod = 1e9 + 7;
const long double PI = acos(-1);
// Super is a cute girl
void solve()
{
    int PA, PB, aa, ab, ba, bb;
    cin >> PA >> PB >> aa >> ab >> ba >> bb;
    long double diffa = hypot(abs(PA), abs(PB));
    long double diffb = hypot(abs(aa), abs(ab));
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--)
        solve();
}