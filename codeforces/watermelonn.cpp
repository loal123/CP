#include <iostream>
using namespace std;
int main()  {
    int a;
    cin >> a ;
    while(true)
    {
        if (a % 2 == 0 && a > 2 )
        cout << "YES";
        else
        cout << "NO";
        return 0;
    }

    
}