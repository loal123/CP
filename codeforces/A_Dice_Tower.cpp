#include <bits/stdc++.h>
using namespace std;
bool check = true;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    int n, m;
    float total = n * m;
    total /= 5;
    trunc(total);
}