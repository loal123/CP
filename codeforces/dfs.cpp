#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
bool visited[88] = {false};
void dfs(int at)
{
    if (visited[at])
    {

        visited[at] = true;
    }
    else
    {
        visited[at] = true;
    }
}

void solve()
{
    int n = 6; // number of nodes
    int g = 7; // adjacency list
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
}