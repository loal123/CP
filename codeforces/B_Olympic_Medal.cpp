#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long double ld;
#define pii pair<int, int>
#define pll pair<ll, ll>
#define vi vector<int>
#define vl vector<ll>
#define fi first
#define se second
#define pb push_back
#define mp make_pair
const ll mod = 1e9 + 7;
const ld PI = acos(-1);
// Super is a cute girl
void solve()
{
    int super, cute, girl;
    ld a[super], b[cute], c[girl];
    cin >> super;
    for (int i = 0; i < super; i++)
    {
        cin >> a[i];
    }
    cin >> cute;
    for (int i = 0; i < cute; i++)
    {
        cin >> b[i];
    }
    cin >> girl;
    for (int i = 0; i < girl; i++)
    {
        cin >> c[i];
    }
    int sl, wlc;
    cin >> sl >> wlc;
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    solve();
}